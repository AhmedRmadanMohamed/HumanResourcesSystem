# ===============================
# = SERVER CONNECTION
# ===============================
server.address=${SERVER_ADDRESS:localhost}
server.port=${SERVER_PORT:7036}

# ===============================
# = DATABASE CONNECTION
# ===============================
spring.datasource.url=${DB_URL:jdbc:mariadb://localhost/humanresources}
spring.datasource.username=${DB_USERNAME:Ahmed}
spring.datasource.password=${DB_PASSWORD:S3cure_HR@Db#2026}

# ===============================
# = JPA / HIBERNATE
# ===============================
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.format_sql=true

# ===============================
# = HIKARI CONNECTION POOL
# ===============================
#spring.datasource.hikari.minimum-idle=5
#spring.datasource.hikari.maximum-pool-size=20
#spring.datasource.hikari.idle-timeout=30000
#spring.datasource.hikari.max-lifetime=1800000
#spring.datasource.hikari.auto-commit=true
#spring.datasource.hikari.transaction-isolation=TRANSACTION_READ_COMMITTED

# ===============================
# = LOGGING
# ===============================
logging.level.com.company.common.aop=DEBUG
logging.level.org.springframework.aop=DEBUG

# ===============================
# = FLYWAY CONFIGURATION
# ===============================
spring.flyway.validate-on-migrate=false
spring.flyway.out-of-order=false
spring.flyway.baseline-on-migrate=true
